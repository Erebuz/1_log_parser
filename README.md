## Log parser


### Description

Simple learning project

### Used:

- **make run** to run parse
- **make lint** to linc code
- **make test** to run tests
- **build_docker** to build docker image
- **build_up_docker_compose** to build and run docker container
